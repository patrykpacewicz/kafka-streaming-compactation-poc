apply plugin: 'application'

mainClassName = 'pl.ppac.poc.kafka.producer.Application'

dependencies {
    compile 'io.codearte.jfairy:jfairy:0.5.3'
}

if ( project.hasProperty("topic") ) { run { args(topic) } }